STL standard template library is a library which contains various classes, namely:

1. container classes:
        -sequential
        -container adapter
        -associative
        -unordered_associative

2. iterator classes

3. algorithms
