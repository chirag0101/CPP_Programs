#include<iostream>
#include<math.h>
using namespace std;

int main(){
    int x;
    
    cout<<"X:";
    cin>>x;
    
    cout<<sqrt(x);                      //when we give a negative number then it gives -nan(not a number) output

    return 0;
}